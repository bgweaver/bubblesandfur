{
  "name": "bubbles-and-fur",
  "version": "1.0.0",
  "description": "Starter template for Bootstrap websites",
  "main": "index.js",
  "scripts": {
    "sass:watch": "sass --no-source-map --quiet-deps --load-path=node_modules --watch scss:css",
    "sass:build": "sass --no-source-map --quiet-deps --load-path=node_modules scss:css"
  },
  "author": "Ben Weaver",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.6.0",
    "bootstrap": "^5.3.3",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "sass": "^1.79.4",
    "terser": "^5.34.1"
  }
}
